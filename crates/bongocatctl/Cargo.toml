[package]
name = "bongocatctl"
version = "3.0.0-dev"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "Cliente de configuración y control de bongocat (TUI, en curso)."

[lints.rust]
unsafe_code = "forbid"

[dependencies]
bongocat-common = { path = "../bongocat-common" }
